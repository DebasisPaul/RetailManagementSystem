<Window x:Class="RMSDesktopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RMSDesktopUI.Views"
        mc:Ignorable="d" FontSize="18"
        Title="ShellView" Height="600" Width="1000">
    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="18">
            <MenuItem Header="_File">

            </MenuItem>
            <MenuItem Header="_Account">
                <MenuItem x:Name="LoginScreen" Header="_Login" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="15*"/>
                <ColumnDefinition Width="178*"/>
            </Grid.ColumnDefinitions>
            <ContentControl x:Name="ActiveItem" Margin="20 5 20 20" Grid.ColumnSpan="3" />
        </Grid>
    </DockPanel>
</Window>
